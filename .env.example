# Postgres (локальний Docker, з docker-compose.yml)
DATABASE_URL=postgresql://psich:psich@localhost:5432/psich?schema=public

# API
NODE_ENV=development
API_PORT=4000
API_HOST=0.0.0.0
CORS_ORIGINS=http://localhost:3000,http://localhost:3001

# JWT (Phase 2)
JWT_ACCESS_SECRET=change-me-access-secret-please-rotate
JWT_REFRESH_SECRET=change-me-refresh-secret-please-rotate
ACCESS_TOKEN_TTL_SECONDS=900
REFRESH_TOKEN_TTL_SECONDS=2592000
COOKIE_SECURE=false
# COOKIE_DOMAIN=psyplatform.example  # set in production

# Web (apps/web)
NEXT_PUBLIC_API_URL=http://localhost:4000

# Admin (apps/admin)
NEXT_PUBLIC_ADMIN_API_URL=http://localhost:4000

# Google OAuth (Phase 2.B.2)
# Set both CLIENT_ID and CLIENT_SECRET to enable /api/auth/google*; leave
# empty and the routes return 503 (the rest of the API keeps working).
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://localhost:4000/api/auth/google/callback
# Where the API redirects the browser back to after the callback. Must point
# at the student-facing web app's origin.
WEB_APP_URL=http://localhost:3000

# LiqPay (Phase 3)
LIQPAY_PUBLIC_KEY=
LIQPAY_PRIVATE_KEY=

# Monobank Acquiring (Phase 3)
MONO_TOKEN=

# Cloudinary (Phase 4)
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# Email (Phase 5/6)
RESEND_API_KEY=
EMAIL_FROM=noreply@psyplatform.local

# Web
NEXT_PUBLIC_API_URL=http://localhost:4000
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Admin
NEXT_PUBLIC_ADMIN_API_URL=http://localhost:4000
